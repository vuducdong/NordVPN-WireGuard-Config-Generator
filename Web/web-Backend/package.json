{
  "name": "nordgen-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "bun -e \"import fs from 'fs'; fs.mkdirSync('assets', { recursive: true })\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "type-check": "wrangler types && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "hono": "4.12.22",
    "hono-rate-limiter": "^0.5.3",
    "zod": "^4.4.3"
  }
}